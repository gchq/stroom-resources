#The file used to define the docker tags to use
bin/local.env
bin/.temp.env

.gradle
classes
build/
classes/
runner/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# We never want to control the contents of these folders
dev-resources/compose/volumes/auth-service/logs
dev-resources/compose/volumes/stroom-auth-db

# IntelliJ
# Note: Deciding what IntelliJ files should be ignored is over-complicated and boring.
# Here we ignore everything and create exceptions for what we want to share.
/out/
/.idea/*
!/.idea/runConfigurations
