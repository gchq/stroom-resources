version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Auth
      - type: volume
        source: stroom-auth-service_logs
        target: ${STROOM_LOG_SENDER_STROOM_AUTH_SVC_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom-auth-service}
