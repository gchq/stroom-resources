version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Auth
      - type: volume
        source: stroom-auth-service_logs
        target: /stroom-log-sender/log-volumes/stroom-auth-service
