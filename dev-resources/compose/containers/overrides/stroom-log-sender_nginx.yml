version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Nginx
      - type: volume
        source: nginx_logs
        target: ${STROOM_LOG_SENDER_STROOM_NGINX_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom-nginx}

