version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom
      - type: volume
        source: stroom_logs
        target: ${STROOM_LOG_SENDER_STROOM_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom}
