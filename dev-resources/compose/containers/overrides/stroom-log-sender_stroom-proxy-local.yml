version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Proxy (local)
      - type: volume
        source: stroom-proxy-local_logs
        target: ${STROOM_LOG_SENDER_STROOM_PROXY_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom-proxy}
