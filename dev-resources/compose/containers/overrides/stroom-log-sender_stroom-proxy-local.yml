version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Proxy (local)
      - type: volume
        source: stroom-proxy-local_logs
        target: /stroom-log-sender/log-volumes/stroom-proxy
