version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Stats
      - type: volume
        source: stroom-stats_logs
        target: ${STROOM_LOG_SENDER_STROOM_STATS_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom-stats}
