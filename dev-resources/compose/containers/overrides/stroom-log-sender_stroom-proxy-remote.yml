services:
  stroom-log-sender:
    volumes:
      # Stroom Proxy (remote)
      - type: volume
        source: stroom-proxy-remote_logs
        target: /stroom-log-sender/log-volumes/stroom-proxy-remote
