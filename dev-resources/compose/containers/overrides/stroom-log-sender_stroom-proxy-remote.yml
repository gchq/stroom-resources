version: '2.4'

services:
  stroom-log-sender:
    volumes:
      # Stroom Proxy (remote)
      - type: volume
        source: stroom-proxy-remote_logs
        target: ${STROOM_LOG_SENDER_STROOM_PROXY_REMOTE_BASE_LOGS_DIR:-/stroom-log-sender/log-volumes/stroom-proxy-remote}
